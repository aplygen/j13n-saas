name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Build config module with correct Maven profile
        run: |
          cd core
          if [ "${{ github.ref_name }}" = "main" ]; then
            PROFILE=production
          else
            PROFILE=develop
          fi
          echo "Using Maven profile: $PROFILE"
          mvn clean package -P$PROFILE -DskipTests

      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker image
        run: |
          ls -la
          pwd
          cd core
          IMAGE_NAME=ghcr.io/${{ github.repository_owner }}/core:${{ github.ref_name }}
          docker build -t $IMAGE_NAME -f core/Dockerfile core/
          docker push $IMAGE_NAME
